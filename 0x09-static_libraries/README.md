# 0x09. C - Static libraries

### 0. Creating static library
* Create the static library libmy.a containing 20 functions

### 1. Script to create static library
* Create a script called create_static_lib.sh that creates a static library called liball.a from all the .c files that are in the current working directory
